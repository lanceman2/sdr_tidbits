
INSTALL_DIR = $(PREFIX)/bin

# harvest this tidbit
tb_8bitsToEachFloat_SOURCES := tb_8bitsToEachFloat.c

INSTALLED := tb_GNURadioScope

tb_GNURadioScope: tb_GNURadioScope.py
	cp $< $@


CLEANFILES := tb_GNURadioScope

include ../quickbuild.make
